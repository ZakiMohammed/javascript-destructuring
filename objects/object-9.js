// to function

const setProduct = ({ name, price, color }) => console.log('Stored:', name, price, color)

const product = {
    id: 1,
    name: 'Woodland Shoes',
    price: 3000,
    color: 'Brown'
}

setProduct(product)